# Benchmark results

Records of `lx` vs. the tools it can replace. The methodology and the
ground rules for a fair number live in [`README.md`](README.md). Each
section below is one `cargo xtask bench` invocation; only compare sections
whose environment headers match.

To add a record:

```sh
cargo build --release
cargo xtask bench >> benchmarking/results.md
```

## Record index

| Date | Machine | lx | Notes |
|---|---|---|---|
| 2026-09-14 | Linux 7.1.9-arch1-2 x86_64 | lx 0.1.0 | Omarchy (Arch-based) host: no dpkg `symbols`/`shlibs` DB, so the `deps resolve` row is skipped; `fpm`, `nfpm`, and `apt-ftparchive` absent. |

## Run 2026-09-14 — x86_64

- Date:        2026-09-14T10:19:35Z
- Host:        Linux 7.1.9-arch1-2 x86_64
- Architecture: amd64
- CPUs:        16
- Timed runs:  5 per tool (1 warm-up)
- Bench dist:  trixie (single-suite deb comparison)
- lx:          lx 0.1.0
- Reference tools:
  - dpkg-deb: Debian 'dpkg-deb' package archive backend version 1.23.7-dirty (amd64).
  - dpkg-shlibdeps: Debian dpkg-shlibdeps version 1.23.7-dirty.
  - dpkg-scanpackages: Debian dpkg-scanpackages version 1.23.7-dirty.
  - apt-ftparchive: not installed
  - fpm: not installed
  - nfpm: not installed

### Pack a .deb from a staged tree

Input: one payload directory (a script, a real ELF, and a data file),
staged fresh per run. `lx` is compared against the reference packagers that
take the same "here are files" input.

`lx` is restricted to one distribution (`--distributions "$BENCH_DIST"`) so
the row is one `.deb` vs. one `.deb`; without it `lx` builds the payload once
per Debian suite.

| Tool | Median (ms) | Min (ms) | Runs | Notes |
|---|---:|---:|---:|---|
| lx build --from-dir | 447 | 445 | 5 | also generates control/changelog/copyright + infers ELF deps |
| dpkg-deb --build | 87 | 84 | 5 | builds the .deb only |
| fpm -s dir -t deb | - | - | - | feature-parity reference packager (not installed) |
| nfpm pkg -p deb | - | - | - | feature-parity reference packager (not installed) |

### Resolve shared-library dependencies

Skipped: this host has no dpkg `symbols`/`shlibs` database (the Debian
files under `/var/lib/dpkg/info/`), which both `lx deps resolve` and
`dpkg-shlibdeps` read. Run on a Debian/Ubuntu host to record this row.

### Build an apt repository index

Input: a directory of three `.deb`s. `lx repo` writes an apt-consumable
index; the reference tools write the `Packages` file itself.

`lx` does more here: it gzips `Packages` and writes a `Release` file with
MD5/SHA1/SHA256 checksums. The reference rows time the `Packages` file
only, so this row is "whole `lx repo` vs. whole reference command".

| Tool | Median (ms) | Min (ms) | Runs | Notes |
|---|---:|---:|---:|---|
| lx repo | 15 | 13 | 5 | writes Packages + Packages.gz + Release |
| dpkg-scanpackages | 115 | 115 | 5 | uncompressed Packages only |
| apt-ftparchive packages | - | - | - | uncompressed Packages only (not installed) |

---

<!-- Generated by benchmarking/run.sh. Append to benchmarking/results.md to
keep a record, and note anything unusual (thermal throttling, a busy machine,
a non-release lx build). -->
